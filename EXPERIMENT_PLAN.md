# 🧪 实验计划与进度同步文档

> ⚠️ **给另一个AI的说明**：你我通过这份文档沟通实验进度，有读有写（git同步）。  
> **请你在每次实验前后更新本文件**，确保实验进度始终准确。

---

## 🎯 总体目标

为陈功的毕设论文提供实验数据，验证"基于机载单目视频的三维重建及其数字地球可视化"方法的有效性。

论文实验分布在两个章节：
- **第3章（方法）**：SLAM3R原文在Replica数据集上的基准结果（引用原文数据，不需要跑）
- **第4章（应用）**：用87帧真实航拍视频跑完整流程，评估对齐精度

---

## 📊 实验数据集

### 1. Replica（室内数据集 — 引用原文数据，不需跑）
| 用途 | 状态 |
|------|------|
| 作为方法基准，放在第3章 | ✅ 引用论文原文，不需额外实验 |

### 2. Flight-01（航拍数据 — 陈功的唯一一组真实数据）
| 属性 | 值 |
|------|-----|
| 视频帧目录 | `dji-str/SRT2EXIF/1/` |
| 帧编号范围 | 198 → 1144（步进11） |
| 总帧数 | **87帧** |
| 时长 | 约10秒 |
| 飞行模式 | 直线飞过 |
| 拍摄地点 | 哈尔滨工程大学校园 |
| GPS信息 | 已在帧EXIF中，可用 `parse_gps.py` 提取 |
| GPS提取结果 | `tmp/gps_path.json` |
| 高度 | 待重建后确认 |

> 后续可以补充更多飞行数据

---

## 🧪 实验列表

### 实验1：87帧全流程重建 ← 当前未开始

**目的**：用87帧完整数据跑SLAM3R离线重建，获取点云和轨迹

**命令**：
```bash
# 离线重建模式
python recon.py --input_dir dji-str/SRT2EXIF/1/ --output_dir tmp/recon_result/
# 具体参数可能需要调整，参考 recon.py 的 help
```

**预期产出**：
- `tmp/recon_result/` 目录下的点云文件（.ply）
- 轨迹文件（类似 `scene_traj.txt`）

**状态**：⏳ 未开始

---

### 实验2：GPS与SLAM轨迹对齐评估

**目的**：用Umeyama算法对齐GPS+SLAM轨迹，评估对齐精度（RMSE）

**命令**：
```bash
python align.py --gps_json tmp/gps_path.json --traj tmp/recon_result/scene_traj.txt
```

**预期产出**：
- `tmp/phase3_transform.json` — 对齐变换矩阵
- 控制台输出：RMSE、尺度因子c

**历史参考**：
- 小规模9帧测试时：RMSE≈21m，尺度因子c≈154.06
- **87帧理论上会更准**，期待RMSE显著低于21m

**状态**：⏳ 待实验1完成后执行

---

### 实验3：点云转GLB模型（用于Cesium）

**目的**：将重建的点云（.ply）转换为Cesium可加载的GLB格式

**方法**：可使用外部工具（如 CloudCompare、pygltflib）或手动转换

**预期产出**：
- `cesium/models/reconstruction.glb`

**状态**：⏳ 待实验1完成后执行

---

### 实验4：Cesium可视化和截图

**目的**：在Cesium数字地球上展示重建效果，为论文4.5.2节配图

**步骤**：
1. 启动Cesium服务器：`cd cesium && python server.py`
2. 打开 `cesium/index.html` 加载重建模型
3. 截图保存

**预期产出**：
- 论文4.5.2节的配图
- 可视化效果展示

**状态**：⏳ 待实验3完成后执行

---

### 实验5：轨迹对齐过程截图（用于第4.3节方法说明）

**目的**：展示GPS对齐的中间过程（初始错位 → 旋转矫正 → 最终对齐）

**方法**：运行 align.py 时保存中间结果的可视化图

**已知资源**：`trans-gps/` 目录下已有5张调试截图（展示对齐过程）

**状态**：✅ 已有截图可复用，无需额外执行

---

## 📋 实验依赖关系

```
实验5（已有截图 ✅）
     │
实验1（87帧重建） ← 当前步骤
     │
     ├──→ 实验2（对齐评估）
     │
     └──→ 实验3（转GLB）
               │
               └──→ 实验4（Cesium可视化）
```

---

## 📝 进度记录区

> 请在每次实验前后更新以下内容。

### 最新进度

| 实验 | 状态 | 备注 |
|------|------|------|
| 实验1：87帧重建 | ⏳ 未开始 | — |
| 实验2：对齐评估 | ⏳ 待实验1 | — |
| 实验3：转GLB | ⏳ 待实验1 | — |
| 实验4：Cesium截图 | ⏳ 待实验3 | — |
| 实验5：轨迹截图 | ✅ 已有 | trans-gps/ 下5张 |

### 实验记录

无（待首次实验后补充）

---

## 🔄 维护说明

**维护者**：在另一台机器上执行实验的AI  
**同步方式**：git commit + push/pull  
**更新时机**：
1. 实验开始前 → 将状态改为 `🔄 进行中`
2. 实验完成后 → 更新产出路径、关键结果、状态改为 `✅`
3. 遇到问题 → 记录在备注中

---

_创建于 2026-05-15 | 初始版本_

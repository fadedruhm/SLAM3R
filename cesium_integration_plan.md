# 将 SLAM3R 成果（带位置信息的模型）集成至 Cesium 数字地球方案设计（分步校验版）

由于 SLAM3R 生成的模型（`recon.glb`）和相机位姿（`scene_traj.txt`）处于局部相对坐标系，并没有真实的物理大小尺度和全球坐标。

为了安全可控且方便排错，结合您的需求，我们将集成流程拆分为**三个独立的可视化校验阶段**。每做一部转换前，都单独放入 Cesium 进行判断。

---

## 前置数据准备
- **模型文件**: `tmp/<run_dir>/recon.glb`
- **相机位姿**: `tmp/<run_dir>/scene_traj.txt` (4x4 c2w外参矩阵序列，其包含的平移向量即表示相机之间的相对走位和尺度)
- **含GPS的原图**: `trans-gps/images/*.JPG`

> 关键经验（本次排坑结论）：
> - CesiumJS 在加载 glTF/glb 时，通常会在内部对模型做“轴校正”（把 glTF 的轴系纠正到 Cesium 内部使用的参考系）。
> - 这会导致一个常见错觉：模型渲染出来是“正确朝向”，但你自己用 `Entity.point` / `polyline` 画出来的轨迹点却“离得很远”。
> - 根因往往不是数据错，而是“模型”和“点”没有走同一条变换链（尤其是：点没有乘上同样的轴校正矩阵）。
> - 解决策略：不要依赖私有字段（例如 `_axisCorrectionMatrix` 这类不保证存在的实现细节）；改为显式、确定性地构造并应用同一份轴校正/旋转链路。

---

## 阶段一：纯局部数据的连带展示与相对位置校验

目的：**验证 SLAM 算出来的点云和相机外参本身是否自洽连贯。**

1. **设定任意锚点**：假定一个全球坐标锚点（例如 `经度0, 纬度0, 高度0` 或随便选在国内某经纬度上）。
2. **加载裸模型**：不进行任何 Umeyama 配准和缩放转换。直接将 `recon.glb` 通过 `modelMatrix` 设置在刚给定的锚点上，缩放值为 1。
3. **加载裸相机（只画平移）**：将 `scene_traj.txt` 中每个 4x4 外参矩阵的平移向量（Translation `[X,Y,Z]`）取出，并用同一个锚点绘制点集/折线。
4. **轴校正一致化（非常重要）**：如果你发现“模型与点相差很远”，优先检查是否遗漏了 Cesium 对 glTF 的内部轴校正。
   - 要点：让“轨迹点”与“模型”使用同一份轴校正/旋转链路。
   - 经验上，最稳的做法是：在你将局部点乘上 `modelMatrix` 之前，先对点乘一个你自己定义的、确定性的 glTF→Cesium 轴校正矩阵（而不是去读取 Cesium 私有字段）。
   - 注意避免重复补偿：如果已经做了轴校正，就不要再额外叠加一个等价的 `X=-90°` 之类旋转，否则会出现“越调越歪”。
5. **用户判断**：在 Cesium 视图内观察，模型和相机的**相对分布**是否对得上。例如：相机是不是都在模型的正面航拍、包围状的相机路线是否符合航拍直觉。

可选（强烈推荐）校验方法：
- 如果导出的 `recon.glb` 内包含“保存相机轨迹时写入的红色相机金字塔”，可以从 glb 里提取这些金字塔 apex 点，作为“真值轨迹”。
- 在 Cesium 里用同样的变换链绘制这组 apex 点：只要 apex 点能和 glb 内红色相机图标完全对齐，就说明你的轴校正/矩阵乘法顺序是正确的。

---

## 阶段二：纯粹原生真实的 GPS 轨迹投射与校验

目的：**单独检查带有 EXIF 的图像文件解析出的全球坐标数据是否有漂移或异常，确认飞行区域。**

1. **提取 EXIF**：通过 Python（使用 `exifread` 等）读取出 `trans-gps/images/` 每张照片的 WGS84 `[Latitude, Longitude, Altitude]` 数据。
2. **纯标注打点**：将点云模型、阶段一生成的临时点全部隐藏。只在地球上利用 Cesium 的 `Entity.point` ，在真实提取出的经纬高对应位置上撒点，并将这些点连成一条 GPS 的线段飞行轨迹。
3. **用户判断**：对照卫星底图和地形数据，人工判断这批图像里的 GPS 数据落在的世界角落及移动路径，是否与当时起飞航拍时的情况和地貌一致。位置是否合理。

---

## 阶段三：相似坐标转换及终极融合呈现

目的：**基于解算出的配准矩阵，实现并验证数字孪生的最终状态。**

由于前两大阶段都已经验证合格，第一阶段的局部相机点与第二阶段真实的 GPS 点理论上形状是极其相似的（只是存在缩放大小、空间旋转姿态、以及地球相对偏移上的不同）。

1. **联合配准**：
   - 提取出局部相机位姿序列 $P_{local}$，和真实的 GPS 墨卡托/ECEF绝对坐标系序列 $P_{gps}$。
   - 使用数学闭式算法（如 Umeyama 算法或 SVD-ICP）来计算这**两组点集最完美的对齐参数**。得到统一的旋转矩阵 $R$、平移矢量 $t$ 以及物理缩放因子 $s$。
2. **矩阵融合生成 `ModelMatrix`**：
   - 取出上述求出的 $R, t, s$，组合出可以将原生局部笛卡尔系映射到真实的数字地球地理坐标系的 $4\times4$ 矩阵 `ModelMatrix`。或者将其换算为针对地球某位置的锚点 `HeadingPitchRoll` 姿态加 `scale` 选项。
3. **加载并呈现**：
   - 使用上述求得的**转换矩阵**去重新挂载 `recon.glb` 以及 第一阶段的相机节点。
4. **用户判断**：一切加载出来后，判断放大后的模型是不是能够完美的覆盖、贴合在原本地图地貌上，相机是不是按照真实的相对轨迹挂载在模型的上方天空等。至此检验整个空间融合转换是否正确。

> 补充建议：阶段三如果再次出现“模型贴图对了但相机点跑飞”的情况，仍优先回到阶段一的轴校正一致化检查。Umeyama 输出的 $R,t,s$ 解决的是“相似变换”，不会自动修复“渲染侧轴校正不一致”。
